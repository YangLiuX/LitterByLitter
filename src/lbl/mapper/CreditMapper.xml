<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lbl.mapper.CreditMapper">
	<insert id="insertCredit" parameterType="lbl.entry.Credit">
		INSERT INTO credit (cuserid,operatetime,reward,operate) VALUES (#{id},NOW(),#{reward},#{operate})
	</insert>

	

</mapper>
